[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pearl-gemm-build-utils"
version = "0.1.0"
description = "Build-time utilities and schemas for pearl-gemm"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.13.4"]

